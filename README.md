# darchwin
A MacOS inspired splash screen for `plymouth` featuring a white loading pill underneath the Arch Linux logo.

## Screenshot


## Install
```bash
cd /usr/share/plymouth/themes && \
sudo git clone https://github.com/areyoufeelingitnowmrkrebs/darchwin && \
sudo plymoth-set-default-theme -R darchwin
```
